<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.cz3002.sharetolearn">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.CourseReviewActivity"></activity>
        <activity
            android:name=".ui.DiscussionPypChatActivity"
            android:label="Learning Forum" />
        <activity
            android:name=".ui.MainFeed"
            android:label="@string/title_activity_main_feed"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".ui.SignUp" />
        <activity android:name=".ui.SignIn" />
        <activity android:name=".ui.Splashscreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>